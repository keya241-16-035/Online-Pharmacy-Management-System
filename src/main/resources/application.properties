# Application Configuration
spring.application.name=pharmacy-management-system
server.port=8080

# ==========================================
# MySQL Database Configuration
# ==========================================
spring.datasource.url=jdbc:mysql://localhost:3306/pharmacy_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Zara383728
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Enable H2 Console for convenience (Optional)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Thymeleaf Template Caching (disable for live dev changes)
spring.thymeleaf.cache=false

# Date and Time Formatters for Web
spring.mvc.format.date=yyyy-MM-dd
